{
  "name": "@viptv/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VIPTV native desktop client for Linux, Windows, and macOS (Tauri v2)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
